- case: map_overloads
  disable_cache: true
  main: |
    from returns.pointfree import map_

    def test(arg: float) -> int:
        ...

    reveal_type(map_(test))
  out: |
    main:6: note: Revealed type is 'Overload(def (function: returns.maybe.Maybe[builtins.float*]) -> returns.maybe.Maybe[builtins.int*], def (function: returns.io.IO[builtins.float*]) -> returns.io.IO[builtins.int*], def [_EnvType] (function: returns.context.requires_context.RequiresContext[_EnvType`665, builtins.float*]) -> returns.context.requires_context.RequiresContext[_EnvType`665, builtins.int*], def [_EnvType, _ErrorType] (function: returns.context.requires_context_result.RequiresContextResult[_EnvType`666, builtins.float*, _ErrorType`667]) -> returns.context.requires_context_result.RequiresContextResult[_EnvType`666, builtins.int*, _ErrorType`667], def [_EnvType, _ErrorType] (function: returns.context.requires_context_ioresult.RequiresContextIOResult[_EnvType`668, builtins.float*, _ErrorType`669]) -> returns.context.requires_context_ioresult.RequiresContextIOResult[_EnvType`668, builtins.int*, _ErrorType`669], def [_ErrorType] (function: returns.result.Result[builtins.float*, _ErrorType`670]) -> returns.result.Result[builtins.int*, _ErrorType`670], def [_ErrorType] (function: returns.io.IOResult[builtins.float*, _ErrorType`671]) -> returns.io.IOResult[builtins.int*, _ErrorType`671], def (function: returns.future.Future[builtins.float*]) -> returns.future.Future[builtins.int*], def [_ErrorType] (function: returns.future.FutureResult[builtins.float*, _ErrorType`672]) -> returns.future.FutureResult[builtins.int*, _ErrorType`672])'


- case: map_variance
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.maybe import Maybe

    class A: ...
    class B(A): ...
    class C(B): ...

    x: Maybe[B]

    def test(a: A) -> C:
        ...

    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.maybe.Maybe[main.C*]'


- case: map_callback_protocol
  disable_cache: true
  main: |
    from typing import Callable, TypeVar
    from returns.pointfree import map_
    from returns.maybe import Maybe

    _ValueType = TypeVar('_ValueType', contravariant=True)
    _NewValueType = TypeVar('_NewValueType', covariant=True)
    _ErrorType = TypeVar('_ErrorType', contravariant=True)

    def ensure_callback(
        f: Callable[[Maybe[_ValueType]], Maybe[_NewValueType]],
        c: Maybe[_ValueType],
    ) -> Maybe[_NewValueType]:
        return f(c)

    def test(arg: int) -> float:
        return arg + 1.5

    reveal_type(ensure_callback(map_(test), Maybe.from_value(1)))  # N: Revealed type is 'returns.maybe.Maybe[builtins.float*]'


- case: map_and_flow
  disable_cache: true
  main: |
    from returns.result import Result
    from returns.functions import identity
    from returns.pointfree import map_
    from returns.pipeline import flow

    def test(arg: int) -> float:
        ...

    def stringify(arg: float) -> str:
        ...

    r: Result[int, str]
    reveal_type(flow(r, map_(test), map_(stringify), identity))  # N: Revealed type is 'returns.result.Result*[builtins.str, builtins.str*]'


- case: map_io
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.io import IO

    def test(arg: float) -> int:
        ...

    reveal_type(map_(test)(IO(1.5)))  # N: Revealed type is 'returns.io.IO[builtins.int*]'


- case: map_maybe
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.maybe import Maybe

    def test(arg: float) -> int:
        ...

    reveal_type(map_(test)(Maybe.from_value(1.5)))  # N: Revealed type is 'returns.maybe.Maybe[builtins.int*]'


- case: map_result
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.result import Result

    def test(arg: float) -> int:
        ...

    x: Result[float, str]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.result.Result[builtins.int, builtins.str*]'


- case: map_ioresult
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.io import IOResult

    def test(arg: float) -> int:
        ...

    x: IOResult[float, str]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.io.IOResult[builtins.int, builtins.str*]'


- case: map_requires_context
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.context import RequiresContext

    def test(arg: float) -> int:
        ...

    x: RequiresContext[str, float]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.context.requires_context.RequiresContext[builtins.str*, builtins.int]'


- case: map_requires_context_result
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.context import RequiresContextResult

    def test(arg: float) -> int:
        ...

    x: RequiresContextResult[bool, float, str]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.context.requires_context_result.RequiresContextResult[builtins.bool*, builtins.int, builtins.str*]'


- case: map_requires_context_ioresult
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.context import RequiresContextIOResult

    def test(arg: float) -> int:
        ...

    x: RequiresContextIOResult[bool, float, str]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.context.requires_context_ioresult.RequiresContextIOResult[builtins.bool*, builtins.int, builtins.str*]'


- case: map_future
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.future import Future

    def test(arg: float) -> int:
        ...

    reveal_type(map_(test)(Future.from_value(1.5)))  # N: Revealed type is 'returns.future.Future[builtins.int*]'


- case: map_futureresult
  disable_cache: true
  main: |
    from returns.pointfree import map_
    from returns.future import FutureResult

    def test(arg: float) -> int:
        ...

    x: FutureResult[float, str]
    reveal_type(map_(test)(x))  # N: Revealed type is 'returns.future.FutureResult[builtins.int, builtins.str*]'
