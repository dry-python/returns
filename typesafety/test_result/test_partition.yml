- case: partition_no_params
  disable_cache: false
  main: |
    from returns.result import Success, Failure, partition

    def some(arg: int) -> tuple[list[int],list[str]]:
      suc = Success(arg)
      failure = Failure('test')

      return partition([suc, failure])

    reveal_type(some(1))  # N: Revealed type is "tuple[list[builtin.int],list[builtin.str]]"

