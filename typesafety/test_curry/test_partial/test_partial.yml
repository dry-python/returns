- case: partial_zero_args
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    reveal_type(partial(two_args))  # N: Revealed type is "def (first: int, second: float) -> str"


- case: partial_single_arg
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    reveal_type(partial(two_args, 1))  # N: Revealed type is "def (second: float) -> str"


- case: partial_all_args
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    reveal_type(partial(two_args, 1, second=0.5))  # N: Revealed type is "def () -> str"


- case: partial_single_named_arg
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    reveal_type(partial(two_args, second=1.0))  # N: Revealed type is "def (first: int) -> str"


- case: partial_positional_arg_by_keyword_makes_rest_kwonly
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    # `first` is a positional argument bound by keyword, so `second`
    # can no longer be passed positionally at runtime and must be
    # reported as keyword-only:
    reveal_type(partial(two_args, first=1))  # N: Revealed type is "def (*, second: float) -> str"


- case: partial_positional_arg_by_keyword_rejects_positional_call
  disable_cache: false
  main: |
    from returns.curry import partial

    def two_args(first: int, second: float) -> str:
        ...

    bound = partial(two_args, first=1)
    bound(second=2.0)  # ok
    bound(2.0)  # E: Too many positional arguments  [misc]


- case: partial_middle_positional_arg_by_keyword_makes_rest_kwonly
  disable_cache: false
  main: |
    from returns.curry import partial

    def multiple(
        first: int,
        second: float,
        third: str,
        fourth: bool,
    ) -> str:
        ...

    # `second` is bound by keyword, so every positional argument after
    # it (`third`, `fourth`) becomes keyword-only, while `first` which
    # comes before the hole is still reachable positionally:
    reveal_type(partial(multiple, second=0.5))  # N: Revealed type is "def (first: int, *, third: str, fourth: bool) -> str"


- case: partial_multiple_args
  disable_cache: false
  main: |
    from returns.curry import partial

    def multiple(
        first: int,
        second: float,
        third: str,
        flag1: bool,
        flag2: bool,
        flag3: type,
    ) -> str:
        ...

    reveal_type(partial(multiple, 1, 0.4, flag3=int, flag2=True))  # N: Revealed type is "def (third: str, flag1: bool) -> str"


- case: partial_not_callable_type
  disable_cache: false
  main: |
    from returns.curry import partial

    curried_int = partial(int, 10)

    reveal_type(curried_int)  # N: Revealed type is "def () -> int"


- case: partial_explicit_noreturn
  disable_cache: false
  main: |
    from returns.curry import partial
    from typing import NoReturn

    def exit(x: int) -> NoReturn:
        ...

    reveal_type(partial(exit, 1))  # N: Revealed type is "def () -> Never"


- case: partial_wrong_argument_types
  disable_cache: false
  main: |
    from returns.curry import partial
    from typing_extensions import Literal

    def multiple(
        first: int,
        second: float,
        flag: Literal[True],
    ) -> str:
        ...

    partial(multiple, 'a', flag=False)
  out: |
    main:11: error: Argument 1 to "multiple" has incompatible type "str"; expected "int"  [arg-type]
    main:11: error: Argument 2 to "multiple" has incompatible type "bool"; expected "Literal[True]"  [arg-type]


- case: partial_too_many_positional_args
  disable_cache: false
  main: |
    from returns.curry import partial
    from typing_extensions import Literal

    def multiple(
        first: int,
        second: float,
    ) -> str:
        ...

    partial(multiple, 1, 2.0, 3)
  out: |
    main:10: error: Too many arguments for "multiple"  [call-arg]


- case: partial_invalid_named_arg
  disable_cache: false
  main: |
    from returns.curry import partial
    from typing_extensions import Literal

    def multiple(
        first: int,
        second: float,
    ) -> str:
        ...

    partial(multiple, missing=1)
  out: |
    main:10: error: Unexpected keyword argument "missing" for "multiple"  [call-arg]


- case: partial_regression618
  disable_cache: false
  main: |
    from typing import Callable, TypeVar
    from returns.curry import partial

    _FirstType = TypeVar('_FirstType')
    _SecondType = TypeVar('_SecondType')

    def test(
        default: _SecondType,
        function: Callable[[_SecondType, _FirstType], _SecondType],
    ):
        reveal_type(partial(function, default))  # N: Revealed type is "def (_FirstType) -> _SecondType"
